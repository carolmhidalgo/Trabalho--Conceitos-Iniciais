def calcular_soma_media(lista):
    soma = sum(lista)
    media = soma / len(lista)
    return soma, media

def substituir_palavra(lista, palavra_antiga, palavra_nova):
    nova_lista = [palavra_nova if palavra == palavra_antiga else palavra for palavra in lista]
    return nova_lista

def gerar_triangulo(num_linhas):
    for i in range(1, num_linhas + 1):
        print('*' * i)


numeros = [1, 2, 5, 8]
soma, media = calcular_soma_media(numeros)
print("Soma:", soma)
print("Média:", media)

palavras = ["jurisprudência", "legislação", "código"]
nova_lista = substituir_palavra(palavras, "jurisprudência", "juizado")
print("Lista Alterada:", nova_lista)

num_linhas = 4
gerar_triangulo(num_linhas)


Código Executado:

Soma: 16
Média: 4.0
Lista Alterada: ['juizado', 'legislação', 'código']
*
**
***
****


** Process exited - Return Code: 0 **
Press Enter to exit terminal
